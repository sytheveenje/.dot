# ~/.tmuxinator/bane.yml

name: bane
root: ~/Sites/bane
pre_window: rbenv shell 2.2.3

windows:
  - vim:
      panes:
        - vim
  - terminal:
      panes:
        - php artisan serve
        - gulp watch
